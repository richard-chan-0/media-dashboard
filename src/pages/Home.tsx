const Home = () => {
    return (
        <div className="flex flex-col items-center m-4 gap-2">
            <h1 className="text-lg">Media Utility Dashboard</h1>
            <p>
                Utility webapp that can rename files into jellyfin compatible
                format
            </p>
        </div>
    );
};

export default Home;
